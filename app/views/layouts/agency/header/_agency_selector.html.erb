<div class="ctx">
  <span class="lbl">Agency</span>
  <select>
    <% current_user.agencies.each do |agency| %>
      <option value="<%= agency.id %>" <%= 'selected' if agency == current_agency %>>
        <%= agency.name %>
      </option>
    <% end %>
  </select>
</div>
